---
scope: Agent System MVP 的 OMP Agent 配置、激活、验证、观察与跨 Session 追溯
purpose: 面向后续实现者的中文 build substrate
altitude: feature
updated: 2026-08-22T05:45
---

- (constraint) clean-slate：现有 Python CAP 仅可作为 Bad Case 证据，不是需求、架构或迁移基线。
- (constraint) MVP 只支持 OMP Agent 单客户端；不得为未承诺的多客户端能力预置进程协议。
- (constraint) 稳定配置只引用 Instructions、Skills、MCP 与任务期输入入口，不复制任务内容或私域原文。
- (constraint) 系统必须区分期望装配、最终结果、运行观察与 Unknown；提供三层验证与跨 Session 可追溯；普通路径最多一次激活确认。
- (decision) 采用 TypeScript 原生 OMP 插件，领域内核保持纯 TypeScript 并通过 ports 与 OMP 适配器隔离；绑定全部 MVP 能力，防止为单客户端 MVP 引入 IPC、重复类型映射和双进程生命周期。
- (direction) 已比较 TypeScript 原生插件、Go/Rust/Python 独立 CLI 加 TypeScript 适配和纯离线配置编译器；独立 CLI 的隔离收益不足以覆盖单客户端 MVP 的协议与分发成本，纯离线方案无法满足运行观察闭环。
- (decision) 采用 Bun 内置 SQLite 作为本机稳定配置修订、激活、差异、观察、验证证据与替代链的唯一持久权威；可读 JSON/Markdown 仅为可重建投影，禁止反向写入或成为第二权威。该决定绑定 FR-3~14，防止跨文件部分提交、并发漂移和跨 Session 丢失。
- (decision) 每次激活是 session-scoped 单元，绑定不可变稳定配置修订、输入引用与指纹、客户端环境证据；运行观察只追加证据，不能反写期望状态，跨 Session 只续接持久事实而不继承旧授权或动态任务状态。
- (decision) 采用六边形模块化单体：domain 不依赖 OMP、SQLite、文件系统或时钟；application commands 是唯一状态变更入口；OMP、SQLite、投影、时钟与指纹为 adapters，依赖方向只向内；同一 Bun 进程部署，不引入服务或 IPC。
- (decision) 单个原生 OMP 扩展提供三类表面：用户命令仅做选择/一次确认/检查，LLM 类型化工具仅做低频配置修订与证据记录，生命周期钩子只读采集观察；三者统一调用 application，禁止直接访问 SQLite。
- (decision) 激活状态机固定为 prepare→confirm-once→apply→observe/reconcile；必需能力、必需输入、实质变化裁决或证据完整性缺失时 fail closed；仅可选项缺失可 degraded，且必须列出差异、Unknown 与受影响能力。
- (decision) SQLite 事务与 OMP 外部副作用不宣称原子：每次操作使用幂等 operation id，副作用后保存可观察结果；失败记录部分结果与差异，重试先 reconcile，禁止以推测回滚冒充成功。
- (decision) Truth 使用显式 Known(value,evidence) 或 Unknown(reason,observedAt) tagged union；null/false 不表达未知。三层验证分别保存，只有三层均有直接证据且通过才可派生 Verified。
- (decision) SQLite 使用 strict tables、WAL、单写者序列化、乐观修订条件和事务迁移；可读投影采用 allowlist，私域原文、凭据和动态任务内容不得持久化或写入日志。
- (version) 目标栈经 2026-08-22 官方源核实：Bun 1.3.14；@oh-my-pi/pi-coding-agent 17.4.4；@oh-my-pi/omptype 17.4.4；TypeScript 7.0.2。来源：https://github.com/can1357/oh-my-pi/blob/main/package.json
- (constraint) OMP 官方扩展是 Bun 同进程加载的 TS/JS 模块，可注册 commands、tools 与生命周期事件；扩展无沙箱，必须使用受管 timer、捕获边界且不得 detached promise。来源：https://github.com/can1357/oh-my-pi/blob/main/docs/extensions.md
- (direction) 语言评估按运行环境、客户端集成、类型与测试、分发、长期维护比较：TypeScript 与 OMP/Bun 原生类型和 bun:test 同栈且无需 IPC；Go/Rust 二进制分发强但需新增进程协议与类型映射，Rust 跨编译通常还需 linker；Python 迭代快但 venv 不可搬运且仍需解释器/适配层。来源：https://bun.com/docs/bundler/executables、https://rust-lang.github.io/rustup/cross-compilation.html、https://docs.python.org/3/library/venv.html
- (decision) 运行与分发：用户作用域 OMP Marketplace/Git/本地插件；运行在 OMP Bun 进程内，使用本机 SQLite，不设独立服务、IPC 或遥测；安装/升级与每次激活分离。来源：https://github.com/can1357/oh-my-pi/blob/main/docs/marketplace.md
- (decision) FR-2 固定 CandidateSet→Recommendation→UserDecision：默认 2~3、最多 4 个可区分候选，逐项保存来源、价值、适用边界、差异、依据、风险与 Unknown；只有高风险、权限、不可逆或价值取舍升级。
- (constraint) FR-12 首轮验收固定 T-1/T-2/T-3，缺合适 T-3 时以 T-4 替代并记录理由；每任务 1 基线+2 稳定样本且至少 1 个新 Session；最小样本后只接受、调整、停止或为具名 Unknown 追加一次。
- (decision) 事实模型分别表达配置/安装、发现/启用、连接/批准、本次激活、实际调用/生效；差异分类至少包含缺失、额外、禁用、覆盖、预算/限制裁剪及影响。外部来源安全性、可信度、任务适用性不得由已配置自动推导。
- (event) PRD 与 addendum reconcile 完成：补齐 FR-2、FR-5/6、FR-11/12、FR-13、NFR-6 和 addendum 显式非目标；lint_spine 通过。
- (event) spine finalized：PRD/addendum reconcile 完成；rubric、current-tech、divergence、data-safety 四个独立审查修复后均 PASS；最终 lint_spine 0 findings。
- (source) 研究输入:../../research/technical-three-cli-assembly-architecture-2026-08-22/research.md;2026-08-22;外部窄核心 OMP-first、三端能力差异与语言分发证据。
- (decision) AD-2/AD-3 原位修订:唯一组合根改为外部 TypeScript/Bun CLI;OMP 为首个 client adapter 并保留只转发请求/观察的薄 TS bridge;不建 daemon 或三端同步实现。
- (decision) AD-4 保留 SQLite 产品权威:先前用户明确选择继续有效;研究中的轻量文件 launch index 不足以承载稳定修订、验证、样本与 Bad Case;客户端凭据/transcript/Session 仍不入库。
- (decision) AD-7/8/18 修订为 launch-scoped 激活:确认绑定 operation+plan+manifest;实际 Session locator 启动后取得;证据区分 planned/launched/observed/verified;in-session 切换配置必须新 operation 并重启/恢复。
- (decision) AD-13/14/15 修订:同 native Session resume 用本地 lease;CLI/bridge/client 独立故障域;Bun standalone 与 OMP bridge 分发分离,客户端升级先 probe 和 fresh-resume smoke。
- (decision) 新增 AD-19:AssemblyManifest/AdapterPlan/LaunchReceipt 是唯一跨客户端合同;配置 precedence、trust、hook 与 Session 语义不得进入公共领域模型。
- (decision) 语言决定更新:TypeScript+Bun 外部 CLI 采用;Go 仅在 Bun 分发形成连续两个 release 实证硬门时作为第一反转;Rust/Python/纯脚本不采用。
- (version) 更正旧 version 记录:2026-08-22 稍后复核 OMP moving main 已为 packages 18.0.0、packageManager Bun 1.4.0;旧 17.4.4/1.3.14 快照不再作为当前事实。架构删除具体 main 种子,支持版本仅由 release-pinned probe/smoke 决定。
- (decision) Reviewer fixes:AD-16 固定触发类别与工作/证据;AD-7 新增持久 ChangeAssessment;AD-17 固定完整样本比较键与不可变 ValidationDecision。
- (decision) Reviewer fixes:AD-6/9/14 固定 bridge allowlist、secret-free persisted plan、RuntimeLaunchSpec 调用期边界、原子 invocation 工件、端到端关联、幂等 reconcile 入 SQLite 与清理。
- (decision) Reviewer fixes:AD-8/19 拆分 observationStage 与 validationMethod;capabilityId/subject/requiredness、SourceId 完备 disposition、effect 语义固定。
- (decision) Reviewer fix:AD-13 Session lease 持久 ownerOperationId+单调 fencingToken,仅证明进程树结束后回收;否则 locator blocked 或 fresh/fork。
- (event) architecture update finalized:research integrated;AD-1~AD-19 stable;lint 0 findings;rubric/current-tech/divergence/data-safety reviewers PASS after fixes.
